# v1 — keep it small. Add embeddings/vector store only in Phase 2.
pydantic>=2
streamlit
google-api-python-client
google-auth-oauthlib
# one LLM client — pick your provider:
# openai   (or)   anthropic
