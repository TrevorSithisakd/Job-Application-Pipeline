# v1 — keep it small. Add embeddings/vector store only in Phase 2.
pydantic>=2
openai>=1.0          # used as the client for DeepSeek's OpenAI-compatible API
python-dotenv        # loads secrets from .env so keys stay out of the shell env
google-api-python-client
google-auth-oauthlib

# Local web app (replaces the Streamlit dashboard)
fastapi
uvicorn

# Dev/test only — not needed to run the pipeline.
pytest>=8

# Optional: one-page fill check for rendered resumes (stages/fillcheck.py).
# docx2pdf drives an installed MS Word for docx->pdf; pymupdf renders/counts pages.
docx2pdf
pymupdf
