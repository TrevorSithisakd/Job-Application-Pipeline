# Core runtime — everything needed to run the app + web server.
pydantic>=2
openai>=1.0          # client for DeepSeek's OpenAI-compatible API
python-dotenv        # loads secrets from .env
python-docx          # render.py builds the .docx resume
python-multipart     # FastAPI file uploads (upload-resume endpoint)

# Web app
fastapi
uvicorn

# Gmail ingest
google-api-python-client
google-auth-oauthlib

# Dev + optional tooling live in requirements-dev.txt (tests, exact fill check).
